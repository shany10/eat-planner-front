/**
 * Config Tailwind dédiée au plugin ESLint `eslint-plugin-tailwindcss`.
 *
 * Le plugin attend un chemin via `settings.tailwindcss.config`.
 * On exporte simplement la config Tailwind du projet.
 */

module.exports = require("./tailwind.config.cjs");
