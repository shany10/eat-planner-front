# Port pour le frontend
FRONT_PORT=3001

# URL du backend (utilisé pour proxyfier /api/** depuis le frontend Nuxt)
# En Docker (réseau app-network) le hostname "backend" doit être résolvable.
BACKEND_URL=http://backend:3000
